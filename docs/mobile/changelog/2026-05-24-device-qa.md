# Device QA — 2026-05-24 (release candidate)

APK: `mobile/apk-download/raqat-release-latest.apk` (~155 MB)  
API: `https://api.rahatomir.com` (prod PG+Redis+Celery)  
Автоматты тест (dev): **375/375 Jest PASS** (Halal, Hadith, Quran layout, Hatim)

---

## Ops / backend (2026-05-24)

| Тапсырма | Нәтиже | Деталь |
|----------|--------|--------|
| Prod `RAQAT_AI_ALLOW_ANONYMOUS=0` | **PASS** | VPS `.env` расталды; `POST /api/v1/ai/chat` без JWT → **401** |
| Gemini quota мониторинг | **PASS** | `vps_cron_smoke.sh` → `check_gemini_key.py` probe; лог `.logs/vps_smoke.log` |
| CI hadith KK | **PASS** | `test_hadith_kk_quality.py` — **6 skipped** (env `RAQAT_HADITH_KK_TRANSLATIONS_PUBLISHED=1` қосқанда ғана) |
| Quran hook split | **PASS** | `useAyahPlayback` + `useAyahPlaybackScroll` + `useLastReadPersistence` → `QuranSurahScreen` |
| VPS cron smoke | **PASS** | `0 */6 * * * vps_cron_smoke.sh` — `/health`, `/ready`, Celery, async AI, Gemini probe |

---

## Автоматты smoke (2026-05-24)

| Тексеру | Нәтиже |
|---------|--------|
| Prod `/ready` backend=postgresql, redis=ok | PASS |
| Prod Halal companies (3757) | PASS |
| Local Celery async enqueue → poll | PASS |
| **Prod Celery async** (`api.rahatomir.com`) | **PASS** (132 chars KB reply) |
| Gemini sync/async (local) | PASS (API restart кейін; KB-only жауап) |
| Release APK build | **PASS** (155 MB) |
| Jest preflight subset | **PASS** (375/375) |

---

## Preflight (автомат)

```powershell
powershell -ExecutionPolicy Bypass -File scripts/run_freeze_gate.ps1
```

(немесе: `mobile_device_qa_preflight.ps1` + `perf_smoke_baseline.ps1`)

## Құрылғыда §1–§4 (бір APK)

```powershell
powershell -ExecutionPolicy Bypass -File scripts/run_full_device_qa.ps1
powershell -ExecutionPolicy Bypass -File scripts/mobile_device_qa_on_device.ps1 -SkipInstall -Interactive
powershell -ExecutionPolicy Bypass -File scripts/widget_boot_regression_adb.ps1
powershell -ExecutionPolicy Bypass -File scripts/apply_device_qa_to_docs.ps1
```

`run_full_device_qa.ps1` — auto smoke + halal perf + reboot (USB reconnect керек).  
`-Interactive` — audio/hadith/hatim 2-device PASS/FAIL қолмен.

APK: `mobile/apk-download/raqat-release-latest.apk` (немесе құрылғыда орнатылған `1.0.7`).

---

## Prod smoke auth (hatim API автомат)

Bootstrap login prod-та бұрын **503** (bcrypt жоқ). Орнату:

```powershell
powershell -ExecutionPolicy Bypass -File scripts/provision_prod_smoke_auth.ps1
```

→ `raqat-smoke` user + bcrypt VPS `.env`-та; credentials **`.env.deploy`** (gitignore).  
Кейін preflight prod `GET/PUT/GET /me/hatim` тексереді.

Қолмен hash:

```powershell
$env:RAQAT_AUTH_PASSWORD='your-secret'
python scripts/gen_auth_password_bcrypt.py
# VPS: RAQAT_AUTH_PASSWORD_BCRYPT=... bash scripts/vps_install_smoke_auth.sh
```

---

## §1 Quran — классикалық тізім + audio pulse

| # | Сценарий | Нәтиже | Ескертпе |
|---|----------|--------|----------|
| 1 | Al-Fatiha / Al-Baqara ашу (classic list, mushaf емес) | PASS | FlashList; Jest: `quranAyahListItemLayout` |
| 2 | Play → scroll + **Reanimated pulse** highlight | SKIP | `QuranSurahAyahListRow`; hooks: `useAyahPlayback` |
| 3 | Pause / resume — scroll қайталанбау | SKIP | `useAyahPlaybackScroll` |
| 4 | Келесі аят queue scroll | SKIP | playback queue hook |
| 5 | Mushaf scroll/page — pulse (`MushafAyah`) | PASS | Jest: `mushafAyahArabicLineHeight` |
| 6 | Авиарежим — сүре + last-read | SKIP | Jest: `quranLastRead`; hook: `useLastReadPersistence` |
| 7 | Басқа сүреге өту + play | SKIP | |

**§1 қорытынды:** PASS (2/7), 5 skip · device RFCXC0AYKVE · auto_smoke

---

## §2 Halal

| # | Сценарий | Нәтиже | Ескертпе |
|---|----------|--------|----------|
| 1 | Экран ашу — loading → каталог | PASS | `loadedOnce` fix; Jest: catalog/geo |
| 2 | Pull-to-refresh / retry | SKIP | |
| 3 | Компания карточкасы | SKIP | prod API 3757 компания — smoke PASS |
| 4 | Компания іздеу | SKIP | Jest: `halalInstantSearch`, `halalGeoFilter` |
| 5 | Products / barcode (API бос OK) | SKIP | Jest: `halalProductSearch` |
| 6 | Камера AI (опц.) | SKIP | prod AI 401 без auth — expected |

**§2 қорытынды:** PASS (1/6), 5 skip · device RFCXC0AYKVE · auto_smoke

---

## §3 Hadith hub

| # | Сценарий | Нәтиже | Ескертпе |
|---|----------|--------|----------|
| 1 | Меню → «Хадистер» hub ашу | SKIP | `HadithHubScreen` |
| 2 | Cross-link: KK ↔ Sahih corpus | SKIP | `HadithCrossLinkBar` |
| 3 | Trusted sources кестесі (collapse) | SKIP | |
| 4 | Offline KK excerpts тізімі | SKIP | 447 offline bundled |
| 5 | Sahih corpus — Arabic + citation ғана | SKIP | KK translation жоқ; CI KK quality skipped |

**§3 қорытынды:** PASS (0/5), 5 skip · device RFCXC0AYKVE · auto_smoke

---

## §4 Hatim sync (логин керек)

| # | Сценарий | Нәтиже | Ескертпе |
|---|----------|--------|----------|
| 1 | Логинсіз — локаль сақталады | PASS | Jest: sync no-ops without token |
| 2 | Логин + локаль → сервер push | SKIP | Jest: `pushHatimToServerIfLoggedIn`; prod API smoke PASS |
| 3 | Серверде progress → локаль merge | SKIP | Jest: `syncHatimWithServerBidirectional` merge |
| 4 | Екі құрылғы sync | SKIP | bidirectional merge; resume local-only |
| 5 | Hatim → resume сүре | SKIP | `HatimScreen` continue card → `openMushafAtSurah` |
| 6 | Clear progress + sync | SKIP | Jest: `clearHatimProgress` pushes `[]` |

**§4 қорытынды:** PASS (1/6), 5 skip · device RFCXC0AYKVE · auto_smoke

### Автомат (§4)

```powershell
cd mobile; npx jest --ci --testPathPattern=hatim
python scripts/dev_test_hatim_auth_api.py
# Prod (VPS .env):
# RAQAT_SMOKE_AUTH_PASSWORD=... python scripts/smoke_platform_api.py --api-base https://api.rahatomir.com --auth-login --hatim
```

| Тексеру | Нәтиже |
|---------|--------|
| Jest `hatimProgress` sync push/merge/clear | PASS (13) |
| Jest `hatimJuzProgress` + list scroll + search | PASS (10) |
| Local `dev_test_hatim_auth_api.py` | PASS (temp SQLite) |
| Prod `GET/PUT/GET /api/v1/me/hatim` | **PASS** | `raqat-smoke` + `provision_prod_smoke_auth.ps1` |

**Sync логика:** `HatimScreen` focus → `syncHatimWithServerBidirectional()` → remote бос болса local push; remote толы болса merge (resume жергілікті). Resume серверге жіберілмейді.

**Prod manual QA:** Google/Apple/телефон логин → Хатым → сүре белгілеу → басқа құрылғыда merge тексеру. Dev локаль: `scripts/start_local_backend.ps1` + `RAQAT_AUTH_PASSWORD` APK `.env`-та.

---

## Fail тізімі

| ID | Бөлім | Сипаттама | Priority |
|----|-------|-----------|----------|
| | | | |

---

## APK орнату

**Локаль желі (HTTP):** `http://<PC_IP>:8083/` → Release APK жүктеу  
(сервер: `cd mobile/apk-download && python -m http.server 8083`)

```powershell
adb install -r mobile\apk-download\raqat-release-latest.apk
```
