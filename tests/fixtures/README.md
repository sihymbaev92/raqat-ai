# Test fixtures

Статик HTML/JSON — Python (`tests/test_islamic_kb_home_feed.py`) және mobile Jest (`officialSiteHomeFeed.test.ts`) парсерлерін тексеруге.

Жаңа fixture қосу алдында `data/` орнына осы каталогты қолданыңыз.
