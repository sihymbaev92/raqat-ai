# data/

Жергілікті жұмыс деректері. Репоға кіретіндер:

| Файл | Мақсаты |
|------|---------|
| `hadith_kk_repair_*` | Хадис KK editorial repair pipeline |
| `hadith_sahih_kk_audit_latest.txt` | Сапа аудит |
| `quran_kk_verified.example.json` | Мысал формат |

**Кірмейтіндер** (`.gitignore`): `*-probe*`, `sync-*-log.txt`, `kb-test.txt`, лицензиялық `quran_kk_verified.json`.

HTML fixture-тар: `tests/fixtures/`.
