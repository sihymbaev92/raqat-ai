import type { ImageSourcePropType } from "react-native";

/**
 * Басты бет — намаз уақыты glass карточкасының артқы суреті.
 * Өз суретіңізбен ауыстырыңыз: `mobile/assets/dashboard/prayer_times_hero.png` (немесе .jpg — `require` жолын өзгертіңіз).
 */
export const PRAYER_TIMES_HERO_BG = require("../../assets/dashboard/prayer_times_hero.png") as ImageSourcePropType;

/** Толық «Намаз уақыты» экранының артқы фоны (Мекке / мешіт суреті). */
export const PRAYER_TIMES_SCREEN_BG = require("../../assets/dashboard/prayer_times_screen_bg.png") as ImageSourcePropType;
