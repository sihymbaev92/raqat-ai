/**
 * RAQAT экожүйесіне «барлық дерек дәлелімен» қалай жиналады — техникалық жол картасы.
 *
 * **Құрылғы (телефон):** түпнұсқа метадерек (`provenanceRegistry`) + қолданба көрсететін каталог.
 *
 * **Келешек орталық қабат (сервер):** бір дерек қоймасы, мазмұн бумалары, әріптестер порталы,
 * JSON/API арқылы жаңарту — құрылғы тек «расталған бума» алады.
 *
 * Фазалар:
 * 1. Тіркеу — дерек көзі каталогта (InstitutionSource + DataProvenance).
 * 2. Алу (ingest) — HTTP API, файл импорты, қолмен кураторлық нұсқа.
 * 3. Валидация — лицензия, сілтеме тексеру, мазмұн сәйкестігі.
 * 4. Байлау — әр бумға sourceId, evidenceUrl, recordedAt; қажет болса SHA-256 hash.
 * 5. Сақтау — құрылғыда lineage; серверде — метадерек DB + мазмұн сақинасы.
 * 6. Жариялау — OTA/EAS Update немесе API, қолданба нұсқасын көтеру.
 */

export const PIPELINE_PHASES = [
  "register_source",
  "ingest",
  "validate",
  "attach_provenance",
  "persist",
  "publish",
] as const;
