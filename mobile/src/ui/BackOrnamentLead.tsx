import React from "react";

type Props = {
  iconColor: string;
  iconSize?: number;
};

/** Артқа көрсеткісі өшірілген — қолданбада ← жоқ. */
export function BackOrnamentLead(_props: Props) {
  return null;
}
