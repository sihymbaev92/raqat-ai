/** Background QCF4 JSON/font warm-up on unmetered networks (best-effort). */
export async function prefetchQcf4AssetsOnWifi(): Promise<void> {
  // June 21 baseline: optional prefetch hook; safe no-op until wired to NetInfo.
}
